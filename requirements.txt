# TVM/MLC Core Packages
# 최신 mlc-ai-nightly 설치 (CPU 버전)
# 주의: mlc-ai-nightly는 wheel 파일로 직접 설치하거나 아래 명령으로 설치:
# pip install mlc-ai-nightly -f https://mlc.ai/wheels
# 또는 apache-tvm을 사용하는 경우:
# apache-tvm>=0.14.0

# TVM FFI (Foreign Function Interface)
apache-tvm-ffi>=0.1.8

# Data types for machine learning
ml_dtypes>=0.5.0

# Core Dependencies
numpy>=1.21.0,<2.0.0
scipy>=1.5.0
attrs>=19.3.0
decorator>=4.4.0
psutil>=5.8.0
tornado>=6.0.0
cloudpickle>=1.0.0

# TVM Script Parser
synr==0.6.0

# Type hints
typing-extensions>=4.5.0

# Optional but recommended for Jupyter notebooks
ipython>=7.0.0
jupyter>=1.0.0
matplotlib>=3.3.0

